= form_for (@<%= singular_name %>, html: {class: 'form-horizontal'}) do |f|
  -if @<%= singular_name %>.errors.any?
    #errorExplanation.alert-message.block-message.error
      %h2= "#{pluralize(@<%= singular_name %>.errors.count, "error")} prohibited this <%= singular_name %> from being saved:"
      %ul
        - @<%= singular_name %>.errors.full_messages.each do |msg|
          %li= msg

  %fieldset
    %legend <%= singular_name %>
    <% for attribute in attributes -%>
      .control-group
        = f.label :<%= attribute.name %>, class: 'control-label'
          .controls
            = f.<%= attribute.field_type %> :<%= attribute.name %>
<% end -%>
    .form-actions
      = f.submit 'Save' :class => 'btn primary'
